{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FindElementParams",
  "description": "Params for the jig.findElement command",
  "type": "object",
  "properties": {
    "selector": {
      "type": "object",
      "description": "Selector to match against — see selector.json for full shape"
    }
  },
  "required": ["selector"],
  "additionalProperties": false
}
