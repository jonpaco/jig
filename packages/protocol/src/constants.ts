/** Default WebSocket port for the Jig server. */
export const DEFAULT_PORT = 4042;

/** Default host for connecting to the Jig server. */
export const DEFAULT_HOST = 'localhost';

/** Default timeout for a single connection attempt (ms). */
export const DEFAULT_CONNECT_TIMEOUT = 5000;

/** Default timeout for jig wait — total time to keep retrying (ms). */
export const DEFAULT_WAIT_TIMEOUT = 30000;

/** Default retry interval for jig wait (ms). */
export const DEFAULT_RETRY_INTERVAL = 1000;

/** Default command response timeout (ms). */
export const DEFAULT_COMMAND_TIMEOUT = 30000;

/** Current client protocol version. */
export const CLIENT_VERSION = '0.1.0';
